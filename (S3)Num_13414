import sys
input = sys.stdin.readline

K, L = map(int, input().split())
dic = dict()
for i in range(L):
    b = input().rstrip()
    if b in dic:
        dic.pop(b)
    else:
        pass
    dic[b] = 1
a = list(dic.keys())
for i in range(min(K, len(a))):
    print(a[i])
    
