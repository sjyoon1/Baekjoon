import sys
input = sys.stdin.readline

N, E = map(int, input().split())
D = list(map(int, input().split()))
D.sort()
a = 1
for i in range(1, N):
    if (D[i] - D[i-1]) < E:
        pass
    else:
        a += 1
print(a)
