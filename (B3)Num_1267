import math
N = int(input())
a = list(map(int, input().split()))
b = [0, 0]
for i in a:
    b[0] += (math.ceil((i+1)/30)*10)
    b[1] += (math.ceil((i+1)/60)*15)
if b[0] > b[1]:
    print("M", b[1])
elif b[0] == b[1]:
    print("Y M", b[0])
else:
    print("Y", b[0])
