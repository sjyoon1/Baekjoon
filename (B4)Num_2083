while True:
    a = list(map(str, input().split()))
    if a == ["#", "0", "0"]:
        break
    elif int(a[1]) > 17 or int(a[2]) >= 80:
        print(a[0], "Senior")
    else:
        print(a[0], "Junior")
