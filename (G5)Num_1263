import sys
input = sys.stdin.readline

N = int(input())
a = []
for i in range(N):
    T, S = map(int, input().split())
    a.append([T, S])
a.sort(key=lambda x: -x[1])
b = 10**7 + 1
for i in a:
    b = min(b, i[1])
    b -= i[0]

if b >= 0:
    print(b)
else:
    print(-1)
