import sys
input = sys.stdin.readline

N = int(input())
H = list(map(int, input().split()))
g = [0] * (10**7 + 1)
for i in range(N):
    g[H[i]] += 1
    if g[H[i]+1] != 0:
        g[H[i]+1] -= 1
print(sum(g))
