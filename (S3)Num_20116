import sys
input = sys.stdin.readline

n, L = map(int, input().split())
x = list(map(int, input().split()))
a = 0
for i in range(n-1, 0, -1):
    a += x[i]
    c = a / (n-i)
    if c <= (x[i-1] - L) or c >= (x[i-1] + L):
        print("unstable")
        exit()
print("stable")
