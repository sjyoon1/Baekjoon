import sys
input = sys.stdin.readline

N = int(input())
a = list(map(int, input().split()))
box = [1 for _ in range(N)]
for i in range(1, N):
    for j in range(i):
        if a[i] > a[j]:
            box[i] = max(box[i], box[j]+1)
print(max(box))
