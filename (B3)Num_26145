import sys
input = sys.stdin.readline

N, M = map(int, input().split())
S = list(map(int, input().split()))
for i in range(M):
    S.append(0)
for i in range(N):
    T = list(map(int, input().split()))
    for j in range(N + M):
        S[j] += T[j]
        S[i] -= T[j]
print(*S)
